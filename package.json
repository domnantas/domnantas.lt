{
  "name": "domnantas.lt",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && subfont -r -i --root dist",
    "preview": "astro preview",
    "lint": "xo"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.5",
    "stylelint-config-xo": "^0.21.0",
    "subfont": "^6.6.0",
    "xo": "^0.49.0"
  },
  "stylelint": {
    "extends": "stylelint-config-xo"
  }
}
